[project]
name = "vaultcal"
version = "0.1.0"
description = "A tiny wizard that prepares a hardened Radicale setup."
authors = [{name = "AxentX"}]
requires-python = ">=3.9"
dependencies = []

[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"
